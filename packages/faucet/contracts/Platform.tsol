pragma ever-solidity >= 0.62.0;

import '@broxus/contracts/contracts/platform/Platform.tsol';

contract FaucetPlatform is Platform {
    constructor(
        TvmCell code,
        TvmCell params,
        address sendGasTo
    ) public Platform (
        code,
        params,
        sendGasTo
    ) {}
}