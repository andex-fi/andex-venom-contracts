pragma ever-solidity >= 0.62.0;
pragma AbiHeader expire;

interface IFaucetFactory {
    function onFaucetDeploy(
        uint54 faucet_deploy_nonce,
        address faucet_owner,
        uint128 claimAmount,
        address claimTokenRoot
    ) external;

    function processUpgradeFaucetRequest(address send_gas_to) external;
    function processUpdateFaucetUserDataRequest(address send_gas_to) external;
}